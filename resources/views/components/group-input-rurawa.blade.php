<div class="flex border border-solid border-secondary-color p-1 rounded-[12px] input">

    <!-- Icon -->
    @if (isset($icon))
        {{ $icon }}
    @endif

    <!-- Input -->
    {{ $input }}

</div>